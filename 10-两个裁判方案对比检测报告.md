# 10 · 两个裁判方案的对比检测报告

> 对比对象：**方案 A = ragas `SQLSemanticEquivalence` + `deepseek-v4-flash`**（原裁判）
> **方案 B = Jev（`~typesafe/jev-latest`）+ OpenRouter 决策接口**（候补裁判）
>
> 检测数据：63 条评测集，`eval/results_20260919_171653.csv` 的 `query / reference_sql / predicted_sql`
> 判定金标：`execution_accuracy`（真跑数仓比对结果集）
> 检测时间：2026-09-21

---

## 1. 结论速览

| 维度 | 方案 A：ragas + DeepSeek | 方案 B：Jev | 谁更好 |
|---|---|---|---|
| 判定准确率（vs 执行结果） | 0.873 | **0.984** | Jev |
| F1 | 0.931 | **0.992** | Jev |
| 误判分布 | FN=8（8 条等价被判不等价） | FN=1 | Jev |
| 误判方向 | 系统性偏严 | 唯一误杀与 ragas 一致 | — |
| 单次请求延迟（串行实测均值） | 2.21s | **0.84s** | Jev（快 2.6 倍） |
| 输入 token | ~780/条 | 1890/条 | ragas |
| 输出 token | ~1215/条（含推理 984） | **免费** | Jev |
| 63 条实测总花费 | 需按 DeepSeek 刊例价计（账单未取） | **$0.005003** | Jev |
| 输出是否可读（理由/解释） | **有，可复盘** | 只有概率 + 归因维度 | ragas |
| 判定结果是否带置信度 | 无（0/1） | **有（校准概率）** | Jev |

一句话：**判定质量、速度、成本 Jev 全面占优；ragas 唯一不可替代的是"会写字"——
能给出人能读的判否理由。** 但第 6 节会说明：这个优势里藏着一个不能忽略的口径问题，
Jev 的高分有一部分是金标口径送的。

---

## 2. 两个方案分别是什么

### 2.1 方案 A：ragas `SQLSemanticEquivalence` + DeepSeek

通用聊天模型做裁判：把"标准 SQL / 生成 SQL / 建表 DDL"拼进 prompt，
让模型**先写一段分析，再输出结构化结论**（等价 / 不等价 + 理由）。

- 接入：`eval/run_ragas_eval.py` 里的 `SQLSemanticEquivalence`，prompt 换成自定义的
  `Nl2SqlEquivalencePrompt`（口径：别名、列顺序、行顺序不影响等价，与 `execution_accuracy` 对齐）
- 关键参数：`max_tokens = 4096`（ragas 默认 1024 会截断结构化输出，这点踩过坑）
- 输出：0/1 + 一段自然语言理由（`sql_equivalence_reason` 列）

### 2.2 方案 B：Jev（`~typesafe/jev-latest`）

TypeSafe 的 **System One 模型**：**不生成文本**，只把一段 state 和一组预定义问题
转成带**校准概率**的类型化答案（是/否、多选一、打分）。一次请求里所有问题并行评估。

- 接入：OpenRouter 决策接口 `POST https://openrouter.ai/api/alpha/decisions`，
  实现在 `eval/jev_judge.py`（两个入口共用一份，避免漂移）
- 判定口径：**只用一个总体问题** `same_result`（"两条 SQL 返回的数据集合是否相同"）的概率过阈值
- 另有 6 个原子问题（表/聚合/过滤/分组/LIMIT/列），**只用于失败归因，不参与判定**
- 输出：概率 + 判否维度 + 理由串（`jev_equivalence` / `jev_probability` / `jev_reason` 三列）

### 2.3 两者拿到的输入是同一份

为保证对照公平，两个裁判拿到**同样的问句、标准 SQL、生成 SQL、建表 DDL**。
`run_ragas_eval.py` 里 `--judge both` 时，DDL 变量是同一个。

---

## 3. 检测方法

### 3.1 数据与金标

- 样本：63 条，`predicted_sql` 非空（系统二轮评测的真实产出）
- 金标：`execution_accuracy` = 真跑数仓比对结果集（忽略行序、浮点取 6 位精度）。
  分布：**correct 62 / incorrect 1**
- 为什么用它当金标：这是 NL2SQL 领域公认主指标，不依赖任何模型判断

### 3.2 四种判定口径

| 口径 | 说明 |
|---|---|
| A · ragas | DeepSeek 生成解释 + 结构化结论，≥0.5 判等价 |
| B1 · Jev 总体（无 DDL） | 总体概率 ≥0.5；state 只含问句 + 两条 SQL |
| B2 · Jev 总体（带 DDL，默认） | 同上，state 额外含建表 DDL |
| B3 · Jev 原子合成 | 6 个原子全部 ≥0.5 才判等价（官方 Composite scoring 思路） |

B2 是 `--judge jev` 接入 `run_ragas_eval.py` 后的**实际默认配置**。

### 3.3 复现命令

```bash
# 全量重判（不跑系统，只读上一轮 CSV，约 1 分钟）
python -m eval.jev_equivalence_judge -r eval/results_20260919_171653.csv            # 无 DDL
python -m eval.jev_equivalence_judge -r eval/results_20260919_171653.csv --with-ddl # 带 DDL

# 跑系统 + 两个裁判同时判（--limit 2 冒烟约 1 分钟）
python -m eval.run_ragas_eval --judge both --limit 2
```

---

## 4. 判定质量对比（63 条，金标 = 执行结果）

| 判定口径 | acc | precision | recall | F1 | TP | FP | FN | TN |
|---|---|---|---|---|---|---|---|---|
| A · ragas（DeepSeek） | 0.873 | 1.000 | 0.871 | 0.931 | 54 | 0 | 8 | 1 |
| B3 · Jev 原子合成（无 DDL） | 0.873 | 1.000 | 0.871 | 0.931 | 54 | 0 | 8 | 1 |
| B3 · Jev 原子合成（带 DDL） | 0.889 | 1.000 | 0.887 | 0.940 | 55 | 0 | 7 | 1 |
| B1 · Jev 总体（无 DDL） | 0.968 | 1.000 | 0.968 | 0.984 | 60 | 0 | 2 | 1 |
| **B2 · Jev 总体（带 DDL，默认）** | **0.984** | 1.000 | 0.984 | **0.992** | 61 | 0 | 1 | 1 |

四条硬结论：

1. **四个口径 FP 全是 0**——没有一条"实际不等价"被误判成等价，
   唯一真正失败的用例（排除条件 + 汇总，系统真实短板）两个裁判都抓到了。
   **误判全在反方向：把等价判成不等价（FN）。**
2. **Jev 总体判断比 ragas 少 7 个误杀**（FN 8 → 1），这是 F1 0.931 → 0.992 的全部来源。
3. **原子合成比总体判断差**（F1 0.940 vs 0.992）：拆成 6 个原子后，
   "等价的不同写法"会被原子判否（详见 4.1）。
4. **带 DDL 比不带 DDL 更准**（F1 0.984 → 0.992）：概率均值 +0.015，
   9 条提升、2 条下降、52 条基本不变。时间口径类问句受益最明显。

### 4.1 为什么原子拆分反而更差

原本以为拆细会更稳，实测相反。原因是原子问题要求"表与关联键一致""聚合方式一致"，
而等价写法差异会被判否：

| 用例 | 被判否的原子 | 实际情况 |
|---|---|---|
| 2025年每个月的订单量是多少 | `same_tables` | 一方 JOIN `dim_date` 用 `month` 列，另一方用日期函数，数据相同 |
| 一共有多少笔订单 | `same_agg` | `COUNT(*)` 与 `COUNT(order_id)` |
| 男女客户分别下了多少单 | `same_filter` | 分组写法不同，筛出的记录集合相同 |

总体问题问的是"数据集合是否相同"，天然容忍写法差异——**这正好是项目定的判定口径**。
所以：判定用单点总体概率，原子降级为归因工具。这也说明"拆得越细越好"不普适，
取决于拆解后子问题是否与最终口径同向。

---

## 5. 分歧分析：7 条逐条看

一致率：**88.89%（56/63）**（带 DDL 版；无 DDL 版 90.48%）。
**7 条分歧方向完全一致：全是 Jev 判等价、ragas 判不等价，而执行结果是 correct。**

| # | 问句 | Jev 概率 | ragas | 执行结果 | Jev 归因维度 |
|---|---|---|---|---|---|
| 1 | 2025年每个月的订单量是多少 | 0.63 | 0 | correct | `same_tables` |
| 2 | 男女客户分别下了多少单 | 0.58 | 0 | correct | `same_filter` |
| 3 | 2025年1月与3月的销售额相差多少 | 0.51 | 0 | correct | — |
| 4 | 2025年平均每天有多少笔订单 | 0.54 | 0 | correct | `same_tables` |
| 5 | 2025年订单量最多的月份是哪个月，该月有多少笔订单 | 0.92 | 0 | correct | — |
| 6 | 平均每笔订单卖出多少件商品（保留两位小数） | 0.90 | 0 | correct | `same_agg` |
| 7 | 2025年3月比2月的订单量多了多少笔 | 0.95 | 0 | correct | — |

注意 #3、#4 概率落在 0.51~0.54——**接近阈值，属于"勉强判等价"**，
这类样本适合用低置信度规则再复核一次（见第 9 节）。

### 5.1 分歧实例一：`2025年3月比2月的订单量多了多少笔`

```sql
-- reference
SELECT SUM(CASE WHEN d.month = 3 THEN 1 ELSE 0 END)
     - SUM(CASE WHEN d.month = 2 THEN 1 ELSE 0 END) AS order_growth
FROM fact_order f JOIN dim_date d ON f.date_id = d.date_id
WHERE d.year = 2025

-- predicted（差异：COALESCE + 多了 month IN (2,3)）
SELECT COALESCE(SUM(CASE WHEN d.month = 3 THEN 1 ELSE 0 END), 0)
     - COALESCE(SUM(CASE WHEN d.month = 2 THEN 1 ELSE 0 END), 0) AS order_count_diff
FROM fact_order f JOIN dim_date d ON f.date_id = d.date_id
WHERE d.year = 2025 AND d.month IN (2, 3)
```

ragas 判否的理由写得很清楚：*"It uses COALESCE to return 0 instead of NULL when no rows match."*
**这句话是对的**——无数据时一个返回 NULL、一个返回 0。但在当前数据（2025 年 1~3 月都有订单）上，
两者结果相同，所以 `execution_accuracy = correct`，Jev 给 0.95 判等价。

### 5.2 分歧实例二：`各会员等级分别下了多少单`（两个裁判都判否，但执行是 correct）

```sql
-- reference：INNER JOIN，只返回有订单的会员等级
SELECT c.member_level, COUNT(f.order_id) AS order_count
FROM fact_order f JOIN dim_customer c ON f.customer_id = c.customer_id
GROUP BY c.member_level

-- predicted：LEFT JOIN，会带上无订单的等级（count = 0）
SELECT dc.member_level, COUNT(fo.order_id) AS order_count
FROM dim_customer dc LEFT JOIN fact_order fo ON dc.customer_id = fo.customer_id
GROUP BY dc.member_level ORDER BY dc.member_level
```

ragas 判否，理由同样站得住：*"returns every member_level present in dim_customer,
including member levels that have no matching orders."*
Jev 也给 0.39 判否（归因 `same_tables`）。**但执行结果是 correct**——
因为当前数据里恰好每个会员等级都有订单，多出来的 0 值行不存在。

---

## 6. 一个必须说清的口径问题：金标与 Jev 同向

第 5 节两条实例指向同一件事，这是本次检测**最重要的发现**：

> **ragas 判否的理由往往在语义上成立，只是这些差异在当前数据集上不产生不同结果。**

存在两种不同的"等价"：

| 口径 | 定义 | 谁在用 |
|---|---|---|
| **执行等价** | 在当前数据上跑，结果集相同 | `execution_accuracy`（金标）、Jev 总体问题 |
| **语义等价** | 换任何数据都返回相同结果 | ragas 的实际判法（更严） |

由此产生一个**对本次对比不利的结构性偏差**：
**金标（执行等价）与 Jev 的判断口径同向，与 ragas 的口径不同向。**
所以第 4 节里 Jev 的高分，**有一部分是金标口径送的，不完全是模型能力差距**。

证据：5.2 那条用例里，ragas 与 Jev **同时判否**，而金标 correct——
若按语义等价定义，这条金标本身就标错了（LEFT JOIN 换份数据就会多出 0 值行）。

**正确的读法**：

- 若评测目标是"系统在**这份数据**上答对没有"（NL2SQL 主流口径，也是本项目的目标）
  → 用 Jev，分数可信、成本极低。
- 若评测目标是"生成的 SQL 在**任意数据**下都对"（更严格的正确性）
  → ragas 的判否是有价值的**风险信号**，不能当误判丢掉。

**所以两者不是简单的替代关系，而是可以互补**——见第 10 节建议。

---

## 7. 成本与延迟（实测）

### 7.1 延迟（同机、同网络、串行 5 条取均值，排除并发排队干扰）

| 项 | 方案 A：ragas | 方案 B：Jev |
|---|---|---|
| 单次请求均值 | 2.21s | **0.84s** |
| min / max | 1.63s / 3.48s | 0.66s / 1.37s |
| 批量表现 | 12 条全并发墙钟 5.9s | 63 条并发 5：单条墙钟均值 5.42s（含排队），加总 341s，实际墙钟约 68s |

Jev 单次请求快约 **2.6 倍**，且方差更小（0.66~1.37s vs 1.63~3.48s）。

### 7.2 Token 与成本

| 项 | 方案 A：ragas | 方案 B：Jev |
|---|---|---|
| 输入 token | ~780/条（DDL + 两条 SQL，直连实测 `prompt_tokens`） | 1,890/条（带 DDL）/ 1,289/条（无 DDL） |
| 输出 token | ~1,215/条（其中推理 984），**需付费** | **免费**（决策接口只按输入计） |
| 63 条总花费 | 按 DeepSeek 刊例价计（本次未取账单） | **$0.005003**（带 DDL）/ $0.003413（无 DDL） |
| 单价 | — | $0.0000794/条（带 DDL） |

关键点：**Jev 输入 token 更多（多了 6 个原子问题），但输出免费**；
ragas 输入少一半，却要为"生成解释"这份输出付钱——而这份解释我们只用来人工复盘。
一次 63 条评测，Jev 的裁判成本是**半美分**量级，相对整轮 440+ 次业务 LLM 调用可以忽略。

---

## 8. 概率分布与阈值敏感性

带 DDL 版 63 条的总体概率分布：

| 指标 | 值 |
|---|---|
| min / 中位数 / max | 0.070 / **0.970** / 0.990 |
| ≥ 0.9 | 56 条 |
| ≤ 0.1 | 1 条 |
| 落在 [0.4, 0.6] 纠结区 | **3 条** |

**只有 3/63 条落在纠结区间**——概率没有糊在 0.5 附近，说明模型对自己有判断。
这比"调阈值"重要得多：若概率都聚在中间，换什么阈值都救不回来。

阈值稳定性推论：这 3 条之外，阈值在 0.1~0.9 之间怎么取，判定结果都一样。
实测取 0.5；第 5 节的 #3（0.51）、#4（0.54）属于"踩线判等价"，
真要调阈值，建议**向下调到 0.35~0.4**，能把这两条从"勉强等价"变成明确判否（都是 exec correct，
判否即误杀，反而变差）——所以**不建议调**，保持 0.5。

---

## 9. 失败归因：原子问题的真正用途

带 DDL 版，判否用例的归因维度分布：

| 归因维度 | 条数 |
|---|---|
| 无（判等价） | 55 |
| `same_tables`（表 / 关联方式不同） | 4 |
| `same_agg`（聚合方式不同） | 2 |
| `same_filter`（过滤条件不同） | 1 |
| `same_agg` + `same_group` + `same_columns` | 1 |

原子问题不当判定口径，但两个用途很实在：

1. **失败归因**：判否时告诉人是"表不同"还是"聚合不同"，比只有一个概率好排查。
2. **低置信度触发器**：`jev_probability` 落在 [0.4, 0.6] 的样本自动标出来人工复核
   （本次 3 条，占 4.8%，人工成本可接受）。

---

## 10. 能力边界（不能做什么）

### 10.1 方案 B（Jev）的边界

1. **只验证了"SQL 等价判断"这一个任务。** Jev 官方声明 CJK 弱于英文，
   本次中文 SQL 场景表现良好，但不能外推到其他中文任务。
2. **Jev 不生成文本，物理上无法替换业务链路的生成节点**：
   `generate_sql`、`correct_sql`、三路关键词扩展的产出都是文本。
   真正可能替换的是 `filter_table` / `filter_metric` 这类裁剪判断——**尚未验证**。
3. **判否理由的可读性弱于 ragas**：只有概率 + 维度名，没有自然语言解释。
   要人工复盘时，还是得看 ragas 的 `sql_equivalence_reason`。

### 10.2 方案 A（ragas）的边界

1. **系统性偏严**：8 处误杀全部是"等价写法 / 数据巧合"被判否（第 5 节）。
   用作等价率指标会系统性低估系统真实能力（0.857 vs 真实执行准确率 98.4%）。
2. **成本随"解释长度"线性增长**：输出 token 是纯开销，且 `max_tokens=4096` 时必须留够，
   否则截断导致解析失败。
3. **没有置信度**：只有 0/1，无法做"低置信度复核"这类机制。

---

## 11. 选型建议

### 11.1 日常评测：默认方案 B（Jev）

```bash
python -m eval.run_ragas_eval            # --judge jev 是默认值
```

理由：判定口径与 `execution_accuracy` 一致（不低估系统）、快 2.6 倍、
63 条半美分、带概率可筛选低置信样本。

### 11.2 需要人工复盘：方案 A（ragas）

```bash
python -m eval.run_ragas_eval --judge ragas
```

理由：判否时给出人能读的理由，是排查"为什么不等价"的唯一来源。

### 11.3 建议的互补用法（推荐）

```bash
python -m eval.run_ragas_eval --judge both
```

- **Jev 的概率当判定**（主指标）
- **ragas 的反对票当风险提示**：Jev 判等价、ragas 判否的用例（本次 7 条），
  按语义等价定义存在"换数据就错"的风险（如 LEFT JOIN、COALESCE）。
  这些不该被当成误判丢掉，应当**标为"结果正确但写法有隐患"**——
  对 NL2SQL 系统来说，这是比"这次答对"更有价值的信号。
- **低置信样本人工看**：`jev_probability ∈ [0.4, 0.6]`（本次 3 条）。

按这个用法，本次 63 条的真实结论应该是：
**执行准确率 98.4%（62/63），其中 7 条"结果对但写法有隐患"，1 条系统真实失败。**

---

## 12. 与 09 报告 7.3 节的数据差异说明

本次检测补齐了 7.3 节没做的实验，两处数字需要按本报告更新：

| 项 | 09 报告 7.3 节 | 本次检测 |
|---|---|---|
| Jev 判定质量 | F1 0.984 / acc 0.968（**无 DDL** 版本） | 带 DDL（接入后默认配置）：**F1 0.992 / acc 0.984** |
| Jev 单条延迟 | "单条直连约 1.5s，总 12.8s" | 串行单请求均值 **0.84s**；63 条并发 5 实际墙钟约 68s（此前 12.8s 的口径未注明，以本报告为准） |

结论方向不变（Jev 优于 ragas），且**带 DDL 后优势更大**。

---

## 13. 复现清单

```bash
# 1. 带 DDL 全量重判（63 条，约 1 分钟，$0.005）
python -m eval.jev_equivalence_judge -r eval/results_20260919_171653.csv --with-ddl

# 2. 无 DDL 对照
python -m eval.jev_equivalence_judge -r eval/results_20260919_171653.csv

# 3. 跑系统 + 双裁判（冒烟）
python -m eval.run_ragas_eval --judge both --limit 3

# 4. 不调 API 验证流程
python -m eval.jev_equivalence_judge --dry-run --limit 5
```

产出 CSV：`eval/jev_judge_<时间戳>.csv`（逐条概率、原子维度、耗时、token、成本）
与 `eval/results_<时间戳>.csv`（`--judge both` 时同时含 `sql_equivalence` 与 `jev_equivalence`）。

本次检测用到的三份数据文件：

| 文件 | 内容 |
|---|---|
| `eval/results_20260919_171653.csv` | 二轮 63 条评测结果（ragas 口径 + 执行结果） |
| `eval/jev_judge_20260921_093128.csv` | 63 条 Jev 判定（无 DDL） |
| `eval/jev_judge_20260921_095705.csv` | 63 条 Jev 判定（带 DDL，本报告主线数据） |
